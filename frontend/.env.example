# Optional. Leave empty to use same-origin / Vite proxy.
VITE_API_BASE_URL=

# Optional for local dev proxy target
VITE_DEV_BACKEND_URL=http://localhost:8080

# Required to render route maps with Mapbox GL JS.
VITE_MAPBOX_ACCESS_TOKEN=
